Communica Social is a full stack web app comprisedof a login page, user catalog, and post feed. The app enables users to create, reply to, and view, public posts. The backend is handled with a MongoDB database, Node and Express server routing, and a custom CRUD API that sends requests in order to display the cloud-stored data. Each page is dynamically built with JavaScript and React.
